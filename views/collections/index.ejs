<!--   
<%- include('../partials/header1.ejs') %>

<section class="py-5 text-center container">
    <div class="row py-lg-5">
      <div class="col-lg-6 col-md-8 mx-auto">
        <h1 class="fw-light">Welcome To uCollection!</h1>
        <p class="lead text-muted">View statue collections from collectors around the world!</p>
        <p>
          <a href="/collection/new" class="btn btn-primary my-2">Add new collection</a>
        </p>
      </div>
    </div>
</section>

<div class="album py-5 bg-light">
    <div class="container">
      <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-3">
        <% collection.forEach(function(collection){  %>
        <div class="col">
          <div class="card shadow-sm">
            <img src='<%= collection.image %>' class='card-img-top' height="600px">
            <div class="card-body">
               <h5 class = 'card-title'><%= collection.name %></h5> 
              <p class="card-text"><%= collection.date %></p>
              <div class="d-flex justify-content-between align-items-center">
                <div class="btn-group">
                  <a href="/movie/<%= collection._id %>" class="btn btn-sm btn-outline-secondary">View</a>
                  <button type="button" class="btn btn-sm btn-outline-secondary">Edit</button>
                </div>
              </div>
            </div>
          </div>
        </div>
        <% }); %>
      </div>
    </div>
  </div>

<%- include('../partials/footer.ejs') %> -->